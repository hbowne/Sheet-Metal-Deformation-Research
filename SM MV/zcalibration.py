from calibration import calibrate
import numpy as np

w = 85.20
h = 100.07
l1 = 19.70
l2 = 19.70

ca = np.array([h+l2/2, w/2, 0])
cb = np.array([h/2, -l1/2, 0])
cc = np.array([-l2/2, w/2, 0])
cd = np.array([h/2, w+l1/2, 0])
cm = np.array([h/2, w/2, 0]) 

#F-T Application

center = np.array()
px = 
py =  